import { expenseFynixes } from "./expense-fynixes";
import { expenseModes } from "./expense-modes";
import { expenseTags } from "./expense-tags";

export const CategoryQuery = {
  expenseTags,
  expenseModes,
  expenseFynixes,
};
